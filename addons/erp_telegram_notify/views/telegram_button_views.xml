<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_telegram_button_list" model="ir.ui.view">
        <field name="name">telegram.button.list</field>
        <field name="model">telegram.button</field>
        <field name="arch" type="xml">
            <list editable="bottom">
                <field name="sequence" widget="handle"/>
                <field name="name"/>
                <field name="model_id" options="{'no_create': True}"/>
                <field name="method"/>
            </list>
        </field>
    </record>

    <record id="view_telegram_button_form" model="ir.ui.view">
        <field name="name">telegram.button.form</field>
        <field name="model">telegram.button</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="name" placeholder="e.g. Approve"/>
                        <field name="model_id" options="{'no_create': True}"/>
                        <field name="model_name" invisible="1"/>
                        <field name="method" placeholder="action_approve"/>
                        <field name="sequence"/>
                        <field name="active" invisible="1"/>
                    </group>
                    <div class="alert alert-warning" role="alert">
                        Only methods listed here can be triggered from Telegram, and each
                        one runs as the Odoo user linked to the sender — so access rights
                        and record rules still decide whether it goes through.
                    </div>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_telegram_button" model="ir.actions.act_window">
        <field name="name">Action Buttons</field>
        <field name="res_model">telegram.button</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>
